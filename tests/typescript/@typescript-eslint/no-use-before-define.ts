const x = Foo.FOO;

enum Foo {
  FOO,
}

let myVar: StringOrNumber;
type StringOrNumber = string | number;

let var1: StringOrNumber2;
type StringOrNumber2 = string | number;

let var2: Enum;
enum Enum {}